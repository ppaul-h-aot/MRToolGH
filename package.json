{
  "name": "github-pr-comment-tool",
  "version": "1.0.0",
  "description": "GitHub PR Comment Tool - Monitor actionable comments across repositories",
  "main": "github-server.js",
  "scripts": {
    "start": "node github-server.js",
    "dev": "nodemon github-server.js",
    "fetch-data": "node data-fetcher.js --fetch-now",
    "start-fetcher": "node data-fetcher.js",
    "start-all": "bash start-all.sh",
    "build": "echo 'No build step needed'",
    "deploy": "bash deploy.sh"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "helmet": "^7.1.0",
    "express-basic-auth": "^1.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
